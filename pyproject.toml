[project]
name = "for-internship"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cloudpickle>=3.1.1",
    "pyspark==3.5.0",
    "pytest>=8.4.2",
]
